\PassOptionsToPackage{unicode=true}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[10pt,letterpaper]{article}

\usepackage[top=0.43in,bottom=0.43in,left=0.58in,right=0.58in]{geometry}
\usepackage{fontspec}
\usepackage{xcolor}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage[hidelinks]{hyperref}

\defaultfontfeatures{Ligatures=TeX}
\setmainfont{Arial}

\definecolor{ResumeBody}{HTML}{181818}
\definecolor{ResumeName}{HTML}{191919}
\definecolor{ResumeSection}{HTML}{282828}
\definecolor{ResumeSectionRule}{HTML}{454545}
\definecolor{ResumeContact}{HTML}{373737}
\definecolor{ResumeTitle}{HTML}{3E3E3E}
\definecolor{ResumeDates}{HTML}{464646}
\definecolor{ResumeEduSecondary}{HTML}{4B4B4B}
\definecolor{ResumeCompanyMeta}{HTML}{505050}

\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\setcounter{secnumdepth}{-1}
\pagestyle{empty}

$if(subtitle)$
\newcommand{\resumesubtitle}{$subtitle$}
$else$
\newcommand{\resumesubtitle}{}
$endif$

\color{ResumeBody}

\renewcommand{\maketitle}{%
  \begin{center}
    {\fontsize{20.5}{20.5}\bfseries\color{ResumeName}\MakeUppercase{$title$}\par}
    \vspace{3pt}% name → subtitle: slightly more breathing room
    {\fontsize{11.5}{12.075}\bfseries\color{ResumeTitle}\MakeUppercase{\resumesubtitle}\par}
    \vspace{0pt}% subtitle → contact: keep tight (contact follows immediately)
  \end{center}
}

% Section headings: 12pt (larger than company name), underline #454545
\titleformat{\section}
  {\fontsize{12}{12.6}\bfseries\color{ResumeSection}}
  {}
  {0em}
  {\MakeUppercase}
  [\vspace{1pt}{\color{ResumeSectionRule}\titlerule[0.65pt]}\vspace{2.5pt}]

\titlespacing*{\section}{0pt}{6pt}{0pt}

\titleformat{\subsection}
  {\fontsize{12}{12.6}\bfseries\color{ResumeSection}}
  {}
  {0em}
  {\MakeUppercase}
  [\vspace{1pt}{\color{ResumeSectionRule}\titlerule[0.65pt]}\vspace{2.5pt}]

\titlespacing*{\subsection}{0pt}{6pt}{0pt}

\renewcommand{\emph}[1]{{\fontsize{9.5}{10.26}\selectfont\color{ResumeDates}\itshape #1}}

\setlist[itemize]{
  topsep=2pt,
  partopsep=0pt,
  itemsep=2.25pt,
  parsep=0pt,
  leftmargin=0.31in,
  labelsep=0.08in,
  labelwidth=0.11in,
  itemindent=0pt,
  label=\textbullet,
  before={\fontsize{9.5}{10.545}\selectfont\color{ResumeBody}},
  after=\vspace{3pt}
}

\providecommand{\tightlist}{%
  \setlength{\itemsep}{2.25pt}\setlength{\parskip}{0pt}}

\setlength{\emergencystretch}{3em}

$for(header-includes)$
$header-includes$
$endfor$

\begin{document}
$if(title)$
\maketitle
$endif$

$body$

\end{document}
