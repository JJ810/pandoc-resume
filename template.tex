\PassOptionsToPackage{unicode=true}{hyperref}
\PassOptionsToPackage{hyphens}{url}
\documentclass[10pt,letterpaper]{article}

\usepackage[top=0.58in,bottom=0.58in,left=0.62in,right=0.62in]{geometry}
\usepackage{fontspec}
\usepackage{xcolor}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage[none]{hyphenat}
\usepackage{needspace}
\usepackage[hidelinks]{hyperref}

\defaultfontfeatures{Ligatures=TeX}
\setmainfont{Arial}

\definecolor{ResumeBody}{HTML}{181818}
\definecolor{ResumeName}{HTML}{191919}
\definecolor{ResumeSection}{HTML}{282828}
\definecolor{ResumeSectionRule}{HTML}{888888}
\definecolor{ResumeContact}{HTML}{555555}
\definecolor{ResumeTitle}{HTML}{3E3E3E}
\definecolor{ResumeDates}{HTML}{555555}
\definecolor{ResumeEduSecondary}{HTML}{4B4B4B}
\definecolor{ResumeCompanyMeta}{HTML}{505050}

\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\setcounter{secnumdepth}{-1}
\pagestyle{empty}
\raggedbottom

% Strongly discourage hyphenation for ATS-safe text extraction
\hyphenpenalty=10000
\exhyphenpenalty=10000
\setlength{\emergencystretch}{2em}
\tolerance=2000
\hbadness=10000

$if(subtitle)$
\newcommand{\resumesubtitle}{$subtitle$}
$else$
\newcommand{\resumesubtitle}{}
$endif$

\color{ResumeBody}
\fontsize{10}{10.5}\selectfont

\renewcommand{\maketitle}{%
  \begin{center}
    {\fontsize{24}{24}\bfseries\color{ResumeName}\MakeUppercase{$title$}\par}
    \vspace{4pt}
    {\fontsize{12}{12.6}\bfseries\color{ResumeTitle}\resumesubtitle\par}
    \vspace{1pt}
  \end{center}
}

% Section headings ~12pt, lighter rule, more breathing room
\titleformat{\section}
  {\fontsize{12}{12.6}\bfseries\color{ResumeSection}}
  {}
  {0em}
  {\MakeUppercase}
  [\vspace{1.5pt}{\color{ResumeSectionRule}\titlerule[0.4pt]}\vspace{4pt}]

\titlespacing*{\section}{0pt}{9pt}{0pt}

\titleformat{\subsection}
  {\fontsize{12}{12.6}\bfseries\color{ResumeSection}}
  {}
  {0em}
  {\MakeUppercase}
  [\vspace{1.5pt}{\color{ResumeSectionRule}\titlerule[0.4pt]}\vspace{4pt}]

\titlespacing*{\subsection}{0pt}{9pt}{0pt}

% Dates: muted italic
\renewcommand{\emph}[1]{{\fontsize{10}{10.5}\selectfont\color{ResumeDates}\itshape #1}}

% Compact bullets: ~1.25em left margin, 1.5–2.5pt between items
\setlist[itemize]{
  topsep=2pt,
  partopsep=0pt,
  itemsep=2pt,
  parsep=0pt,
  leftmargin=1.25em,
  labelsep=0.4em,
  labelwidth=0.6em,
  itemindent=0pt,
  label=\textbullet,
  before={\fontsize{10}{10.5}\selectfont\color{ResumeBody}},
  after=\vspace{4pt}
}

\providecommand{\tightlist}{%
  \setlength{\itemsep}{2pt}\setlength{\parskip}{0pt}}

$for(header-includes)$
$header-includes$
$endfor$

\begin{document}
$if(title)$
\maketitle
$endif$

$body$

\end{document}
